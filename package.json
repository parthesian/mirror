{
  "name": "mirror-gallery",
  "version": "1.0.0",
  "description": "Photo gallery application",
  "scripts": {
    "dev": "npx wrangler pages dev . --live-reload",
    "mock": "node scripts/mock-photos-server.mjs",
    "build": "node build.js",
    "generate:d1-import": "node scripts/generate-d1-import.mjs",
    "test": "node scripts/verify-gallery-order.mjs"
  },
  "engines": {
    "node": ">=14"
  },
  "dependencies": {
    "jose": "^6.2.2",
    "three": "^0.160.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "wrangler": "^4.80.0"
  }
}
